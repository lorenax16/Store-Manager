const sinon = require('sinon');

const salesProduct = require('../../../services/salesServices');

describe('teste camada service do sales', () => {
  describe('testando se cria uma venda', () => {
    
  })
})